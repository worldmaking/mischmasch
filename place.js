// calculate jack placement along panel given panel width

let panelWidth = 5;

let placement = (panelWidth / 2) * -1 + 0.5

// placement = Math.sign(placement) * Math.round(Math.abs(placement))
console.log(placement)